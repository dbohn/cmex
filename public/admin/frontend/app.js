// Generated by CoffeeScript 1.6.2
(function() {
  define(['jquery', 'underscore', 'backbone', 'toolbar', 'pageman', 'editors/InlineEditor'], function($, _, Backbone, ToolbarView, PageMan, InlineEditor) {
    var initialize;

    initialize = function() {
      $(function() {
        var chunks, pageman, textblocks, tools;

        tools = new ToolbarView({
          page: cmexPage
        });
        tools.render();
        $('body').append(tools.el);
        pageman = new PageMan({
          toolbar: tools,
          page: cmexPage
        });
        chunks = $('[about]');
        textblocks = chunks.filter('[typeof="text.html"]');
        _.each(textblocks, function(block) {
          var bl;

          bl = new InlineEditor(block);
        });
      });
    };
    return {
      initialize: initialize
    };
  });

}).call(this);
