// Generated by CoffeeScript 1.6.2
(function() {
  define(['jquery', 'underscore', 'backbone', 'toolbar', 'pageman', 'ChunkDetector'], function($, _, Backbone, ToolbarView, PageMan, ChunkDetector) {
    var initialize;

    initialize = function() {
      $(function() {
        var chunkdetector, pageman, tools;

        tools = new ToolbarView({
          page: cmexPage
        });
        tools.render();
        $('body').append(tools.el);
        pageman = new PageMan({
          toolbar: tools,
          page: cmexPage
        });
        chunkdetector = new ChunkDetector();
        chunkdetector.detect();
      });
    };
    return {
      initialize: initialize
    };
  });

}).call(this);
